<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    {{#if seo_desc}}
        <meta name="description" content="{{seo_desc}}">
    {{/if}}
    <link rel="icon" href="/img/icons/tetrark_ico.jpg">
    <link rel="stylesheet" href="{{css_file}}">
</head>
<body>
{{> header}}

<main class="wrapper">
    {{{body}}}
</main>

{{> footer}}
</body>
</html>